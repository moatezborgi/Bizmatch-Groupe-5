import { CustomSearchFilterPipe } from './custom-search-filter.pipe';

describe('CustomSearchFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomSearchFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
